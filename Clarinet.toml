[project]
name = "continuous-checkpoint-authorizer"
authors = ["Project Team"]
description = "A decentralized system for continuous progress verification and milestone tracking"
telemetry = false
requirements = []
[contracts.checkpoint-authorizer]
path = "contracts/checkpoint-authorizer.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
